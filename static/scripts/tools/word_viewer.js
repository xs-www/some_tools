// tool specific script for templates/tools/word_viewer.html
(function(){
  function init(){
    // placeholder: tool page init logic
    console.log('word_viewer script loaded')
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init)
  else init()
})()
